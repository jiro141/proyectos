# IP del servidor backend en la red local
# Cambiá esto por la IP de la máquina donde corre el backend Django
# Ej: VITE_API_URL=http://192.168.1.100:8000
# Dejalo vacío si usás el mismo dispositivo para backend y frontend
VITE_API_URL=
